import { database } from "firebase";

declare var $ENV: ENV;

interface ENV {
    apiKey: string,
    authDomain: string,
    databaseURL: string,
    projectId: string,
    storageBucket: string,
    messagingSenderId: string,
    appId: string
}